@page
@model SuccessModel
@{
    ViewData["Title"] = "Upload Successful";
}

<div class="portal-container">
    <div class="portal-card">
        <div class="portal-header">
            <div class="success-icon">✅</div>
            <h1>Upload Successful</h1>
            <p class="subtitle">@Model.UploadedFiles.Count file(s) stored securely.</p>
        </div>

        <div class="success-details">
            <dl>
                @foreach (var (fileName, blobName) in Model.UploadedFiles)
                {
                    <dt>@fileName</dt>
                    <dd>@blobName</dd>
                }
            </dl>
        </div>

        <div class="security-badge">
            <span>🔒 Stored encrypted in Azure Blob Storage</span>
        </div>

        <div style="text-align:center">
            <a href="/Upload" class="btn-secondary">Upload another file</a>
        </div>
    </div>
</div>
